/**
 * Barril de `ProfileSectionsLayout`. Expone el componente y el tipo
 * `ProfileSection` (necesario para que quien lo monta construya el array
 * `sections`) — no exporta `ProfileSectionsLayoutProps` (CLAUDE.md §14.5).
 */
export { ProfileSectionsLayout, type ProfileSection } from './ProfileSectionsLayout';
