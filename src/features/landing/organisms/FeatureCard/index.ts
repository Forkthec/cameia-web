/**
 * Barril de `FeatureCard`. Expone `FeatureCard` para quien lo consuma desde
 * fuera de esta carpeta (hoy, solo `LandingPage`). No exporta
 * `FeatureCardProps` (CLAUDE.md §14.5).
 */
export { FeatureCard } from './FeatureCard';
