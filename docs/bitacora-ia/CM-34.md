## Tarea 1 · 19-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Registro con Firebase Authentication (HU-1.1)

**Petición/prompt relevante (resumen).** Implementar `PRT-01.01 · Registro` (`/registro`) siguiendo
SDD-Anchored, en modo plan: `src/features/auth/SPEC.md` (sección Registro) y
`docs/adr/0006-registro-orquestado-por-backend-via-post-users.md` ya existían, escritos por el
usuario en una sesión previa de investigación (sin bitácora propia — este archivo no existía en
disco pese a que el prompt de la tarea asumía una "Entrada 1"; se crea aquí siguiendo la
convención real de nombre de este directorio, `CM-<numero>.md`, no
`CM-34-registro-firebase.md`). Fase 0 pedía verificar cinco puntos puntuales contra el código real
antes de escribir nada, y contrastar el DTO/controlador reales de `cameia-cuentas` (adjuntados por
el usuario) contra lo que la SPEC asumía.

**Resultado.**

- Fase 0 confirmó: `httpClient.ts` ya soporta peticiones sin token; `calculateAge.ts` no tiene otro
  consumidor; no existe `auth.service.test.ts` ni ninguna prueba de `signUp()`; `errors.json` no
  tenía colisión con `REGISTRO_CORREO_DUPLICADO`; `AUTH_EMAIL_TAKEN`/`AUTH_INVALID_EMAIL`/
  `AUTH_WEAK_PASSWORD` eran exclusivos de `signUp()`, sin otro consumidor.
- **Hallazgo grande, reportado antes de tocar nada:** los nombres de campo que `SPEC.md`/`ADR-0006`
  asumían como provisionales (`phone`, `pronouns` plural) no coinciden con el DTO real
  (`RegisterUserRequest.java`): son `phoneNumber` y `pronoun` (singular); `birthDate` viaja como
  `dd/MM/yyyy`, no ISO. El usuario confirmó el cambio y además entregó el enum real
  `tech.cameia.cuentas.domain.model.Pronoun` (`HE`/`SHE`/`THEY`, coincide con la propuesta previa) y
  señaló que `RegisterUserService.register()` lanza tres excepciones de dominio, no una
  (`EmailAlreadyRegisteredException`, `InvalidBirthDateException`, `WeakPasswordException`) — la
  segunda no estaba contemplada en el plan inicial y se agregó con el mismo tratamiento visual que
  "menor de edad" de cliente; la tercera quedó como Bloqueo B-12 nuevo (sin copy).
- `SPEC.md` actualizado en el mismo commit que el resto (`CLAUDE.md` §16): nombres de campo
  corregidos en §3/§5, B-09 resuelto citando `Pronoun.java`, B-12 nuevo, tabla de errores de §4
  ampliada con `REGISTRO_FECHA_NACIMIENTO_INVALIDA`, nota informativa sobre
  `AccountActivationController` (fuera de alcance de `CM-34`, territorio `HU-1.2`), §7 completado
  con los archivos reales de esta iteración, `estado` de IMPLEMENTADA (antes EN_CURSO).
- Construido, en orden `model/` → `schemas/` → `api/` → `organisms/` → `pages/` → `hooks/`:
  `model/pronouns.ts`; `schemas/register.schema.ts` (cuatro causas de `fechaNacimiento` vía
  `.superRefine`, mismo patrón que `educationSchema`); `api/register.dto.ts`/`register.mapper.ts`/
  `register.api.ts`; `mocks/handlers/auth.handlers.ts` ganó su primer handler real (no era un
  archivo nuevo, existía vacío); `utils/calculateAge.ts` ganó `isFutureDate`/`isImplausiblyOld`;
  `design-system/organisms/Modal/Modal.tsx` (primer `Modal` de la app, backdrop como `<button
  aria-hidden>` en vez de `<div onClick>` para cumplir `jsx-a11y` sin desactivar reglas);
  `organisms/RegisterForm/RegisterForm.tsx`; `hooks/useRegister.ts` (con el caso de borde de
  `signIn()`/`sendEmailVerification()` fallando tras un `POST` exitoso, redirigiendo a `/ingresar`
  con `location.state.registerInfo` — requirió agregar `infoMessage` a `LoginForm`/`LoginPage`, no
  previsto en el plan original pero necesario para que ese caso de borde tuviera dónde mostrarse);
  `pages/RegisterPage.tsx` (reemplaza el placeholder); `services/firebase/auth.service.ts` retira
  `signUp()` y las 3 entradas exclusivas de `FIREBASE_ERROR_CODE_MAP`, agrega
  `sendEmailVerification()`; `routes.tsx` corregido para que `RegisterPage` componga su propio
  `AuthLayout` (ya no queda envuelta desde fuera, mismo patrón que `LoginPage`); i18n
  (`auth.json`/`errors.json`) reestructurado (`nombreCompleto` se retira, se separa en
  `nombre`/`apellido`, según ya anotaba `SPEC.md` §9).
- Verificación real: `pnpm typecheck` (0 errores), `pnpm lint` (0 errores, 2 warnings preexistentes
  no relacionados con esta rama), `pnpm test` (94 archivos / 382 pruebas, todas pasan),
  `pnpm format:check` (0 issues tras un `prettier --write` puntual sobre 4 archivos nuevos),
  `pnpm spec:check` (0 errores — encontró y corrigió 4 errores reales preexistentes en la tabla del
  §7 de `SPEC.md`, de una sesión anterior: rutas de archivo sin el prefijo `src/`, y `/ingresar`/
  `/registro` entre backticks que el script interpretaba como rutas de archivo verificables por
  contener "/"; se corrigieron quitando backticks a los identificadores de ruta/endpoint que no son
  archivos).

**Qué se aceptó, corrigió o rechazó, y por qué.**

- El usuario confirmó, con evidencia (el código real de `cameia-cuentas`), los tres puntos de pausa
  que este proceso señaló explícitamente en el plan: (1) retirar `signUp()` y sus 3 entradas
  exclusivas del mapa de errores, con las líneas exactas mostradas antes de tocarlas — ninguna
  prueba se retiró, confirmado en Fase 0; (2) construir `Modal` con el copy provisional del Plan
  Gratis (Bloqueo B-08 sigue abierto); (3) corregir los nombres de campo/formato de fecha en
  `SPEC.md` antes de escribir `register.dto.ts`, con el backend como fuente de verdad.
- Se agregaron dos piezas que el plan original no había previsto y que solo aparecieron al
  contrastar contra el backend real: el manejo de `InvalidBirthDateException` (mismo tratamiento
  visual que "menor de edad" de cliente, no una alerta genérica) y el mensaje informativo de
  `LoginForm`/`LoginPage` para el caso de borde de `useRegister` — ninguno se inventó sin
  respaldo: el primero viene del propio código de `RegisterUserService.java` que el usuario
  adjuntó, el segundo ya estaba descrito en `SPEC.md` §3 como "decisión de Frontend", solo faltaba
  el punto de la UI donde mostrarlo.
- No se implementó `POST /api/v1/users/me/verification` (`AccountActivationController`): confirmado
  como territorio de `HU-1.2`, fuera de alcance de `CM-34` — se dejó como nota informativa en
  `SPEC.md`, no como código.
- El error de `jsx-a11y` en el backdrop de `Modal` no se resolvió desactivando la regla: se
  rediseñó como un `<button>` real (`tabIndex={-1}`, `aria-hidden`) detrás del cuadro de diálogo,
  manteniendo el cierre con Esc (documento) y clic en el velo, sin duplicar lógica de
  `stopPropagation`.
- **Verificación de UI en navegador no realizada** (sin herramienta de automatización de navegador
  en este entorno): la implementación se validó con `pnpm typecheck`/`lint`/`test`/`format:check`/
  `spec:check` y con pruebas de integración contra el MSW real (`RegisterPage.test.tsx`), no con
  una interacción real en un navegador — mismo límite ya documentado en `CM-40.md`.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request, incluida la verificación manual en navegador (`lg`/`sm` contra los node-ids de
`SPEC.md` §3) que esta sesión no pudo completar.
