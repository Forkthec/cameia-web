## Tarea 1 · 19-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Registro con Firebase Authentication (HU-1.1)

**Petición/prompt relevante (resumen).** Implementar `PRT-01.01 · Registro` (`/registro`) siguiendo
SDD-Anchored, en modo plan: `src/features/auth/SPEC.md` (sección Registro) y
`docs/adr/0006-registro-orquestado-por-backend-via-post-users.md` ya existían, escritos por el
usuario en una sesión previa de investigación (sin bitácora propia — este archivo no existía en
disco pese a que el prompt de la tarea asumía una "Entrada 1"; se crea aquí siguiendo la
convención real de nombre de este directorio, `CM-<numero>.md`, no
`CM-34-registro-firebase.md`). Fase 0 pedía verificar cinco puntos puntuales contra el código real
antes de escribir nada, y contrastar el DTO/controlador reales de `cameia-cuentas` (adjuntados por
el usuario) contra lo que la SPEC asumía.

**Resultado.**

- Fase 0 confirmó: `httpClient.ts` ya soporta peticiones sin token; `calculateAge.ts` no tiene otro
  consumidor; no existe `auth.service.test.ts` ni ninguna prueba de `signUp()`; `errors.json` no
  tenía colisión con `REGISTRO_CORREO_DUPLICADO`; `AUTH_EMAIL_TAKEN`/`AUTH_INVALID_EMAIL`/
  `AUTH_WEAK_PASSWORD` eran exclusivos de `signUp()`, sin otro consumidor.
- **Hallazgo grande, reportado antes de tocar nada:** los nombres de campo que `SPEC.md`/`ADR-0006`
  asumían como provisionales (`phone`, `pronouns` plural) no coinciden con el DTO real
  (`RegisterUserRequest.java`): son `phoneNumber` y `pronoun` (singular); `birthDate` viaja como
  `dd/MM/yyyy`, no ISO. El usuario confirmó el cambio y además entregó el enum real
  `tech.cameia.cuentas.domain.model.Pronoun` (`HE`/`SHE`/`THEY`, coincide con la propuesta previa) y
  señaló que `RegisterUserService.register()` lanza tres excepciones de dominio, no una
  (`EmailAlreadyRegisteredException`, `InvalidBirthDateException`, `WeakPasswordException`) — la
  segunda no estaba contemplada en el plan inicial y se agregó con el mismo tratamiento visual que
  "menor de edad" de cliente; la tercera quedó como Bloqueo B-12 nuevo (sin copy).
- `SPEC.md` actualizado en el mismo commit que el resto (`CLAUDE.md` §16): nombres de campo
  corregidos en §3/§5, B-09 resuelto citando `Pronoun.java`, B-12 nuevo, tabla de errores de §4
  ampliada con `REGISTRO_FECHA_NACIMIENTO_INVALIDA`, nota informativa sobre
  `AccountActivationController` (fuera de alcance de `CM-34`, territorio `HU-1.2`), §7 completado
  con los archivos reales de esta iteración, `estado` de IMPLEMENTADA (antes EN_CURSO).
- Construido, en orden `model/` → `schemas/` → `api/` → `organisms/` → `pages/` → `hooks/`:
  `model/pronouns.ts`; `schemas/register.schema.ts` (cuatro causas de `fechaNacimiento` vía
  `.superRefine`, mismo patrón que `educationSchema`); `api/register.dto.ts`/`register.mapper.ts`/
  `register.api.ts`; `mocks/handlers/auth.handlers.ts` ganó su primer handler real (no era un
  archivo nuevo, existía vacío); `utils/calculateAge.ts` ganó `isFutureDate`/`isImplausiblyOld`;
  `design-system/organisms/Modal/Modal.tsx` (primer `Modal` de la app, backdrop como `<button
  aria-hidden>` en vez de `<div onClick>` para cumplir `jsx-a11y` sin desactivar reglas);
  `organisms/RegisterForm/RegisterForm.tsx`; `hooks/useRegister.ts` (con el caso de borde de
  `signIn()`/`sendEmailVerification()` fallando tras un `POST` exitoso, redirigiendo a `/ingresar`
  con `location.state.registerInfo` — requirió agregar `infoMessage` a `LoginForm`/`LoginPage`, no
  previsto en el plan original pero necesario para que ese caso de borde tuviera dónde mostrarse);
  `pages/RegisterPage.tsx` (reemplaza el placeholder); `services/firebase/auth.service.ts` retira
  `signUp()` y las 3 entradas exclusivas de `FIREBASE_ERROR_CODE_MAP`, agrega
  `sendEmailVerification()`; `routes.tsx` corregido para que `RegisterPage` componga su propio
  `AuthLayout` (ya no queda envuelta desde fuera, mismo patrón que `LoginPage`); i18n
  (`auth.json`/`errors.json`) reestructurado (`nombreCompleto` se retira, se separa en
  `nombre`/`apellido`, según ya anotaba `SPEC.md` §9).
- Verificación real: `pnpm typecheck` (0 errores), `pnpm lint` (0 errores, 2 warnings preexistentes
  no relacionados con esta rama), `pnpm test` (94 archivos / 382 pruebas, todas pasan),
  `pnpm format:check` (0 issues tras un `prettier --write` puntual sobre 4 archivos nuevos),
  `pnpm spec:check` (0 errores — encontró y corrigió 4 errores reales preexistentes en la tabla del
  §7 de `SPEC.md`, de una sesión anterior: rutas de archivo sin el prefijo `src/`, y `/ingresar`/
  `/registro` entre backticks que el script interpretaba como rutas de archivo verificables por
  contener "/"; se corrigieron quitando backticks a los identificadores de ruta/endpoint que no son
  archivos).

**Qué se aceptó, corrigió o rechazó, y por qué.**

- El usuario confirmó, con evidencia (el código real de `cameia-cuentas`), los tres puntos de pausa
  que este proceso señaló explícitamente en el plan: (1) retirar `signUp()` y sus 3 entradas
  exclusivas del mapa de errores, con las líneas exactas mostradas antes de tocarlas — ninguna
  prueba se retiró, confirmado en Fase 0; (2) construir `Modal` con el copy provisional del Plan
  Gratis (Bloqueo B-08 sigue abierto); (3) corregir los nombres de campo/formato de fecha en
  `SPEC.md` antes de escribir `register.dto.ts`, con el backend como fuente de verdad.
- Se agregaron dos piezas que el plan original no había previsto y que solo aparecieron al
  contrastar contra el backend real: el manejo de `InvalidBirthDateException` (mismo tratamiento
  visual que "menor de edad" de cliente, no una alerta genérica) y el mensaje informativo de
  `LoginForm`/`LoginPage` para el caso de borde de `useRegister` — ninguno se inventó sin
  respaldo: el primero viene del propio código de `RegisterUserService.java` que el usuario
  adjuntó, el segundo ya estaba descrito en `SPEC.md` §3 como "decisión de Frontend", solo faltaba
  el punto de la UI donde mostrarlo.
- No se implementó `POST /api/v1/users/me/verification` (`AccountActivationController`): confirmado
  como territorio de `HU-1.2`, fuera de alcance de `CM-34` — se dejó como nota informativa en
  `SPEC.md`, no como código.
- El error de `jsx-a11y` en el backdrop de `Modal` no se resolvió desactivando la regla: se
  rediseñó como un `<button>` real (`tabIndex={-1}`, `aria-hidden`) detrás del cuadro de diálogo,
  manteniendo el cierre con Esc (documento) y clic en el velo, sin duplicar lógica de
  `stopPropagation`.
- **Verificación de UI en navegador no realizada** (sin herramienta de automatización de navegador
  en este entorno): la implementación se validó con `pnpm typecheck`/`lint`/`test`/`format:check`/
  `spec:check` y con pruebas de integración contra el MSW real (`RegisterPage.test.tsx`), no con
  una interacción real en un navegador — mismo límite ya documentado en `CM-40.md`.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request, incluida la verificación manual en navegador (`lg`/`sm` contra los node-ids de
`SPEC.md` §3) que esta sesión no pudo completar.

---

## Tarea 2 · 19-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Contraseña/celular reales + contrato de error real

**Petición/prompt relevante (resumen).** El usuario probó `/registro` en su propio navegador tras
la Tarea 1 y recibió `422` al enviar el formulario — no pudo registrarse. Lanzó otro agente con
acceso al gateway y a `cameia-cuentas` a diagnosticar; ese agente encontró que `register.schema.ts`
no replicaba dos reglas reales del backend (contraseña: `PasswordPolicy.java`, ≥12/≤64 caracteres,
rechaza comunes; celular: `PhoneNumber.java`, formato E.164 estricto) y, al revisar
`BusinessExceptionHandler.java`, encontró algo más grande: el formato de error que
`errorMap.ts`/`ApiError.ts` esperaban (`{code, message, details}`) no es el que el backend real
envía (`ProblemDetail`, RFC 7807, sin `code` propio). El usuario pidió: (1) mostrar los errores
reales con advertencia inline, todo por i18n; (2) revisar los dos mockups que faltaban (error
"menor de edad" `73:449` — releído por el helper de celular y el medidor de contraseña que no se
habían construido — y `password-strength` `33:251`); (3) investigar autodetección de indicativo
telefónico; (4) foco + scroll automático al primer error; (5) un plan completo con archivos
afectados y decisiones para que él las respondiera, documentado en modo SDD-Anchored.

**Resultado.**

- **Modo plan, 4 preguntas de arquitectura, las 4 con la opción recomendada elegida por el
  usuario:** (1) reescribir `ApiError`/`errorMap.ts` contra el `ProblemDetail` real ahora, no un
  parche local — arregla `auth` y `professional-profile` a la vez; (2) la escala visual de 4
  niveles del medidor de fuerza la propone Frontend (el backend es binario); (3) instalar
  `libphonenumber-js` y construir el átomo/molécula/organismo de teléfono necesario, con permiso
  explícito para la dependencia nueva y la desviación de Figma; (4) agregar `forwardRef` a
  `Input`/`Select`/`PasswordField` para que el foco automático de `react-hook-form` funcione.
- **Mientras se armaba el plan, el usuario adjuntó los tres archivos Java reales**
  (`PasswordPolicy.java`, `PhoneNumber.java`, `AgePolicy.java`) — lo que iba a quedar como
  "propuesta de Frontend, sin confirmar" pasó a ser regla real confirmada: 12–64 caracteres por
  *code point* + lista cerrada de 32 contraseñas comunes (replicada literal en
  `commonPasswords.ts`); E.164 estricto sin adivinar país; mismo orden/umbrales de edad que ya
  tenía `calculateAge.ts`. El usuario pidió además, no ligado a ningún archivo adjunto: que el
  `<input type="date">` de `fechaNacimiento` no deje elegir un día futuro desde el propio selector
  nativo (`max={hoy}`), no solo al enviar.
- **`ApiError`/`errorMap.ts` reescritos** (`ADR-0007`, nuevo): `httpStatus`/`title`/`detail`/
  `errors: [{field, message}]`, sin `code`. Blast radius real, no solo `auth`:
  `mocks/handlers/profiles.handlers.ts` (~15 sitios), `EditProfilePage.tsx`,
  `missingRequirements.ts`, `useFinalizeProfile.ts`/`profile.api.ts` (TSDoc),
  `professional-profile/SPEC.md` §4 (tabla de errores corregida, los códigos de mock retirados de
  `errors.json` nunca estuvieron confirmados con backend). `errors.json` se recorta a lo
  genuinamente compartido por `httpStatus` (`generico`, `red`, `NOT_FOUND`, los `AUTH_*` de
  Firebase, sistema aparte).
- **Contraseña:** `features/auth/model/commonPasswords.ts` (nuevo, copia literal de
  `PasswordPolicy.COMMON_PASSWORDS`); `register.schema.ts` gana las reglas reales vía
  `.superRefine` (longitud por code point, no `.length()`); `utils/passwordStrength.ts` (nuevo,
  decisión de Frontend, sin `// PROVISIONAL` porque esa marca es para contrato pendiente del
  backend y aquí no hay ninguno); `design-system/molecules/PasswordStrength/` **reescrito por
  completo** (existía desde antes, sin consumidor, con una escala inventada que no coincidía con
  Figma `33:251` — el contrato real es `empty|weak|fair|good|strong`; `strong` reutiliza el token
  `--success-text`, ya existía sin consumidor, no se agregó ninguno nuevo).
- **Celular:** `libphonenumber-js@1.13.13` instalada (`CLAUDE.md` §2); `features/auth/organisms/
  PhoneField/` (nuevo) — selector de país (`Select`, opciones de la librería, nombre vía
  `Intl.DisplayNames`, sin bandera — el design system no tiene esos assets, Bloqueo B-13 nuevo) +
  número nacional (`Input`). Colombia por defecto. **Diferencia consciente con Figma, anotada en
  `SPEC.md` (`CLAUDE.md` §16):** Figma dibuja un solo campo de texto; se reemplaza por el selector
  compuesto porque el usuario lo pidió explícitamente y aprobó la desviación.
- **Foco en errores:** `forwardRef` agregado a `Input`/`Select`/`PasswordField` (aditivo, sin
  consumidor roto); `RegisterForm` pasa `ref={field.ref}` en cada `Controller` — el foco automático
  de `react-hook-form` (`shouldFocusError`, ya activado por defecto) empezó a funcionar solo con
  eso, sin código adicional de foco manual.
- **Mock (`auth.handlers.ts`) reescrito** para replicar el orden real de
  `RegisterUserService.register()` (edad → contraseña → correo duplicado → celular) con la forma
  `ProblemDetail`, incluida la asimetría real: `birthDate`/`password` sí llegan con
  `errors[].field`, el celular (`IllegalArgumentException`) no — su manejador real
  (`valorInvalido()`) no llama `campo()`. Bloqueo B-14 nuevo, documentando esa asimetría.
- Documentación: `CLAUDE.md` §2 (dependencia nueva), §8 (contrato de error real, ya no
  "PENDIENTE"), §17 (fila resuelta, retirada); `docs/adr/0007-contrato-de-error-problemdetail.md`
  (nuevo); `auth/SPEC.md` (§3/§4/§5/§7/§8/§9 actualizados: reglas reales, `PhoneField`, medidor de
  fuerza, `max` de fecha, B-06/B-12 resueltos, B-13/B-14 nuevos); `professional-profile/SPEC.md`
  §4 (tabla de errores corregida al contrato real, sin los códigos de mock nunca confirmados).
- Verificación real: `pnpm typecheck` (0 errores), `pnpm lint` (0 errores, mismos 2 warnings
  preexistentes ajenos), `pnpm test` (97 archivos / 417 pruebas, todas pasan — subieron de 94/382
  de la Tarea 1 por los archivos nuevos y las pruebas de `professional-profile` que sí se ven
  afectadas por el cambio de `ApiError`), `pnpm format:check` (limpio tras un `prettier --write`
  puntual sobre 9 archivos), `pnpm spec:check` (0 errores — corrigió 3 backtick-rutas nuevas que el
  propio script marcó como inexistentes al documentar los archivos de esta tarea).

**Qué se aceptó, corrigió o rechazó, y por qué.**

- Las 4 decisiones de arquitectura se confirmaron explícitamente antes de ejecutarse, en modo plan,
  cada una con su opción recomendada y sus alternativas — ninguna se decidió unilateralmente.
- **El diagnóstico del otro agente se usó como punto de partida, no como verdad final:** su resumen
  de `PasswordPolicy.java`/`PhoneNumber.java` (parafraseado, no el código) se marcó explícitamente
  como pendiente de verificar antes de escribir cualquier regla — el usuario adjuntó los tres
  archivos reales durante la planeación, así que nada quedó basado solo en el resumen del otro
  agente.
- Se descartó pedirle al backend un `codigoCameia` propio y pausar el frontend hasta tenerlo
  (alternativa considerada en el ADR): el `422` real ya bloqueaba el registro en producción, y el
  contrato sin `code` ya era un hecho confirmado en dos microservicios, no una hipótesis por
  esperar.
- Se descartó parchear solo `useRegister`/`register.mapper.ts` sin tocar `errorMap.ts`/`ApiError`
  (más rápido, menor blast radius): habría dejado a `professional-profile` con el mismo problema
  de fondo — su `ApiError.code` nunca tuvo un valor real que leer, solo códigos de mock.
- El texto informal de `WeakPasswordException` ("...brother, cambiala...") no se reutilizó en la
  UI pese a ser ahora un mensaje real y confirmado — CAMEIA usa español neutral/profesional
  (`CLAUDE.md` §1); se escribió un mensaje propio para ese caso.
- El celular no tiene un mensaje de error específico de servidor (a diferencia de contraseña/fecha
  de nacimiento) porque el backend real no lo permite — `valorInvalido()` no etiqueta el campo. No
  se inventó una distinción que la respuesta HTTP no ofrece; se documentó como límite conocido
  (B-14) en vez de forzar un mensaje específico sin respaldo.
- **Verificación de UI en navegador no realizada** (mismo límite que la Tarea 1, sin herramienta de
  automatización en este entorno): la implementación se validó con `pnpm typecheck`/`lint`/`test`/
  `format:check`/`spec:check`, no con una interacción real. El usuario sí probó en navegador y fue
  quien encontró el bug original — su próxima prueba manual es la que cierra el ciclo real.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request, incluida la prueba manual en navegador del flujo completo (contraseña corta/común,
celular con país distinto a Colombia, foco automático en el primer error, correo duplicado) que
esta sesión no pudo completar.

---

## Tarea 3 · 19-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Selector de país compacto + volver a la landing

**Petición/prompt relevante (resumen).** El usuario probó `/registro` en navegador tras la Tarea 2
y adjuntó una captura de pantalla: el selector de país de `PhoneField` mostraba "Colombia (+5"
cortado, y el placeholder del campo de número ("+57 300 000 0000") contradecía al selector de al
lado, que ya mostraba el indicativo. Pidió además que hubiera una forma de volver a la landing (`/`)
desde `/registro` y desde `/ingresar` — no existía ninguna. Pidió explícitamente analizar antes de
tocar código y dar opciones, respetando SDD-Anchored.

**Resultado.**

- **Análisis presentado antes de tocar código:** causa raíz de cada hallazgo (ancho fijo angosto en
  `PhoneField.tsx`, resto literal del campo único que dibujaba Figma antes de partirlo en dos; y,
  verificado en vivo en Figma con `get_metadata` sobre el nodo `94:1124` — ningún frame de Login
  dibuja un elemento de "volver", así que no era una omisión de implementación).
- **Dos preguntas de `AskUserQuestion`:** el usuario **no** eligió la opción recomendada para el
  celular — pidió el selector tipo chip (disparador compacto `CO +57 ▾` con panel y buscador) en
  vez del ajuste mínimo de ancho; sí eligió la recomendada para la navegación (logo + enlace
  explícito "Volver a inicio").
- Por ser un cambio grande (`CLAUDE.md` §13), se armó un plan (`EnterPlanMode`/`ExitPlanMode`) antes
  de escribir código; el usuario lo aprobó con una condición explícita: implementar pero **no hacer
  commit sin su aprobación**.
- **`features/auth/organisms/PhoneField/CountryCodeSelect.tsx`** (nuevo, archivo interno de la
  carpeta del organismo, no exportado desde `PhoneField/index.ts` — único consumidor,
  `CLAUDE.md` §4): disparador `{ISO} +{indicativo}` (p. ej. "CO +57") que nunca se trunca — el ISO
  desambigua indicativos compartidos por varios países (`+1`: EE. UU., Canadá, ~20 del Caribe), sin
  bandera (sin assets, ya revisado en la Tarea 2). Se descartó extender `Combobox`
  (`design-system/molecules/`): su patrón es multi-selección con `Chip`s removibles, no encaja con
  una sola selección detrás de un botón cerrado; el panel interno sí reutiliza su mismo patrón ARIA
  combobox+listbox para el buscador. `PhoneField.tsx` se actualiza para usarlo, y el placeholder del
  número nacional pierde el indicativo redundante (`"300 000 0000"`, ya no `"+57 300 000 0000"`).
- **`src/layouts/AuthLayout.tsx`:** el logo (panel de marca y móvil) ahora enlaza a `ROUTES.landing`
  — mismo carve-out de `boundaries/dependencies` que ya usa `AppShell.tsx` para su wordmark →
  `ROUTES.inicio` (`eslint.config.js`, comentario "AppShell enlaza su wordmark a `ROUTES.inicio`") —
  y se agrega un enlace de texto explícito "Volver a inicio". Se reutilizó la llave de i18n
  `common:acciones.volverInicio` y el destino `ROUTES.landing`, ya usados de forma idéntica en
  `NotFoundPage.tsx` — no se creó ninguna llave nueva para esto.
- Se agregaron dos llaves nuevas en `auth.json` (`registro.celular.buscarPais`,
  `.sinResultados`), siguiendo el mismo criterio de copy específico por pantalla que ya usa
  `profile:rolesObjetivo.agregar.sinResultados` en vez de un mensaje genérico compartido.
- **`src/features/auth/SPEC.md`** actualizado: §2 (corrección del selector de país, con la causa
  raíz del truncado y de la contradicción de copy), §7 (fila nueva de `CountryCodeSelect.tsx`, fila
  de `AuthLayout.tsx` actualizada), §9 (nota del segundo seguimiento, con la verificación en Figma y
  las dos decisiones de `AskUserQuestion`).
- Al reescribir la SPEC se encontró un párrafo con un caracter fuera de lugar (edición concurrente
  del propio usuario en su editor mientras esta sesión trabajaba en el mismo archivo) — se corrigió
  antes de seguir.
- **Verificación inicial:** `pnpm typecheck`/`lint`/`test` (425/425) limpios, `pnpm spec:check` sin
  errores nuevos.
- **Corrección de copy (el usuario probó de nuevo y ajustó `auth.json` directamente):** el helper
  de celular ("Formato internacional, por ejemplo +57 300 000 0000") se partía en dos líneas en
  móvil — se acortó a "ej." en vez de "por ejemplo". El usuario hizo el cambio él mismo en el
  archivo mientras esta sesión trabajaba en `SPEC.md`; se documentó la razón (ancho en móvil, no
  contenido) en vez de revertirlo.
- **Segundo bug real reportado, mismo día — fecha de nacimiento futura en móvil:** el selector
  nativo de fecha permitía elegir un día posterior a hoy. Causa raíz: `max` se calculaba con
  `new Date().toISOString().slice(0, 10)` — convierte a UTC, y Colombia es `UTC-5`, así que de
  7 p. m. a medianoche hora local esa conversión ya devuelve la fecha de mañana. El mismo desfase
  afectaba la propia validación de "fecha futura" en `register.schema.ts` (comparaba contra
  `new Date()` sin normalizar): a esa hora, elegir "mañana" no se marcaba como futuro, porque en
  UTC ya era "hoy" — no era solo un defecto visual del selector nativo, la validación real también
  lo dejaba pasar. Se agregó `utils/calculateAge.ts#todayLocalIsoDate()` (getters locales, nunca
  `toISOString()`) y se corrigieron los dos puntos que usaban la fecha de "hoy" mal calculada:
  `max={todayLocalIsoDate()}` en `RegisterForm.tsx` y la referencia de `isFutureDate()` en el
  `.superRefine` de `register.schema.ts`.
- **Regresión cubierta con manipulación de reloj y huso horario** (primer uso de esta técnica en la
  suite): `vi.stubEnv('TZ', 'America/Bogota')` + `vi.setSystemTime()` fijando la hora a las 8 p. m.
  de Bogotá (1 a. m. UTC del día siguiente) — nuevas pruebas en `calculateAge.test.ts` (contrato de
  `todayLocalIsoDate`) y `RegisterForm.test.tsx` (el `max` no se adelanta; elegir el día siguiente
  sí se marca como fecha futura). Sin la corrección, ambas pruebas fallan exactamente a esa hora —
  no son pruebas que pasen por casualidad según cuándo se ejecute la suite.
- **`SPEC.md`** ganó una segunda ronda de ajustes: §3 (corrección de `max`/`isFutureDate` con su
  causa raíz), §4 (fila de `fechaNacimiento` actualizada), §7 (fila de `calculateAge.ts`
  actualizada), §9 (nota del tercer seguimiento y de la corrección de copy).
- **Quinto pedido, mismo día — mismo límite en el otro extremo:** el usuario pidió, por simetría con
  la fecha futura, que tampoco se pudiera elegir desde el calendario una fecha de nacimiento de más
  de 110 años. Se agregó `utils/calculateAge.ts#oldestPlausibleBirthDateIsoDate()` y el `<input>`
  ganó `min={oldestPlausibleBirthDateIsoDate()}` — mismo límite que ya validaba `isImplausiblyOld`
  al enviar, ahora también bloqueado en el propio selector nativo (móvil y escritorio). El límite no
  es "hace 110 años" tal cual (esa fecha exacta cumple 110, plausible): es un día después de "hace
  111 años", mismo cálculo de cumpleaños que usa `calculateAge`, aplicado al revés. Pruebas nuevas
  en `calculateAge.test.ts` y `RegisterForm.test.tsx` fijan la fecha del sistema y verifican el
  valor exacto del límite (`1915-09-07` para un "hoy" de `2026-09-06`).
- **Verificación final:** `pnpm typecheck`/`lint`/`test` (430/430) limpios, `pnpm format`,
  `pnpm spec:check` sin errores nuevos. **Sin verificación manual en navegador** (mismo límite que
  las tareas anteriores, sin herramienta de automatización en este entorno) — la interacción real
  de abrir el panel de país, el selector de fecha (límite superior e inferior) en un dispositivo
  real, y los dos enlaces de "volver a inicio", quedan pendientes de la prueba del usuario.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente, incluida la
prueba manual en navegador/móvil del selector de país (nombre largo, filtro por indicativo,
teclado), los dos límites del selector de fecha de nacimiento (futura y >110 años), y los dos
enlaces de "volver a inicio" desde `/registro` e `/ingresar`. El commit y el push los hace el
usuario directamente (pedido explícito de esta tarea); esta sesión entrega el mensaje de commit y
el contenido de la plantilla de PR, no los ejecuta.
