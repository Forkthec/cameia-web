## Tarea 1 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5) · Formalizar estándares de código en CLAUDE.md

**Petición/prompt relevante** (texto original, sin editar):

Vas a formalizar los estándares de código del frontend en CLAUDE.md. En este paso NO toques
código fuente ni ningún archivo bajo src/: solo CLAUDE.md.

Antes de escribir, lee: CLAUDE.md, docs/ARCHITECTURE.md, CONTRIBUTING.md, docs/GLOSSARY.md,
docs/adr/README.md y los cinco ADR, docs/_plantilla-feature/SPEC.md, y los documentos de
docs/referencias/ y docs/decisiones/. Ten presente el informe de convenciones observadas que
generaste: es la evidencia de todo lo que sigue.

Regla de esta tarea: no inventes convenciones. Cada cosa que escribas sale del informe de
convenciones, de las instrucciones de abajo, o de los documentos que acabas de leer. Lo que no
encaje en esos tres no se escribe: se deja al final como pregunta para mí.

Dos advertencias:

- La convención de ramas YA está corregida en este archivo por un PR de DevOps. Es
  CM-<numero>-<descripcion-kebab-case>, sin prefijo de tipo. No la toques.
- Varias reglas del archivo describen cosas que todavía no existen en el código, porque los
  pasos que las construían no se han ejecutado. No las borres ni las suavices: márcalas como
  pendientes con el paso que las resolverá.

Propón el plan y espera mi confirmación, como pide §13.

--- A. Sección nueva de estándares de código ---

Redacta como reglas las convenciones que el informe midió sin excepciones:

- Identificadores en inglés; explicaciones, comentarios y llaves de traducción en español.
- export function para componentes; sin arrow, sin export default salvo cuando lo imponga una
  librería.
- Imports en tres grupos sin línea en blanco: externo, alias @/, relativo.
- import type completo cuando el import es solo de tipos; type inline cuando se mezcla con
  valores.
- Las interfaces *Props no se exportan.
- Barriles index.ts solo en la carpeta de cada componente del design system y en la raíz de cada
  feature. No hay barril de categoría ni de carpeta técnica.
- El design system no llama a useTranslation: recibe todo texto visible como prop obligatoria.
  Escribe explícitamente por qué, porque no es evidente: es lo que permite reutilizarlo fuera de
  esta aplicación.
- describe con el identificador bajo prueba en inglés; it en español conjugado en tercera
  persona, sin "should" ni "debería".
- Prueba hermana del archivo probado, sin carpetas **tests**.

--- B. TSDoc ---

Obligatorio en TODOS los archivos, con dos dialectos según el tipo, y dilo así de explícito:

- Módulo con lógica: prosa que explica por qué existe y qué decisión encierra.
- Componente de UI: prosa en la cabecera, más una línea sobre cada prop dentro de la interfaz.
  Sin @param ni @returns: documentar que un componente recibe props no informa de nada.
- Hook o función pura: prosa más @param, @returns y @throws cuando aplique.
- Barril: qué expone y qué deja deliberadamente fuera.
- Prueba: qué regla o contrato protege, y de dónde sale esa regla.

Añade la convención de citas: las reglas duras se citan §3.N, las secciones por su número.

--- C. Correcciones ---

§6 · Fija las unidades que el código ya usa: espaciado en rem, radios en px, tamaño de fuente en
rem, altura de línea sin unidad, espaciado entre letras en em. Nombra el token de área táctil que
va a resolver el requisito de 44 px del §10, y di que llegará en el paso siguiente.

§7 · Los archivos de en/ se borran en el paso siguiente y fallbackLng pasa a operar de verdad;
corrige la afirmación de que "hereda". Elimina el namespace report, que no existe. Las llaves son
camelCase, y UPPER_SNAKE cuando son códigos de dominio; nunca kebab-case. Cambia el ejemplo
obligatorio de catálogo: hoy usa TONOS, que todavía depende del spike CM-21 y que además roza la
regla dura 7. Usa un catálogo ya cerrado, como la forma de respuesta o la modalidad.

§8 · El formato de error acordado es Problem Details, RFC 9457, con extensiones propias.
Documéntalo como el objetivo, y di con todas las letras que errorMap.ts todavía implementa el
formato provisional con code y message, y que la migración está pendiente del contrato real.
No afirmes como implementado algo que no lo está: el CONTRIBUTING lo prohíbe.

§8 · Invierte el orden de construcción de una feature. Hasta ahora era api, model, schemas,
hooks, organisms, pages. El contrato del backend es lo más volátil del proyecto, así que empezar
por ahí es empezar por lo que más cambia. El orden pasa a ser: model, schemas, organisms, pages
contra datos simulados, y al final api con su dto y su mapper. Explica que el mapper es el
cortafuegos: todo lo que el backend pueda cambiar se detiene ahí.

§11 · Son 13 subtareas, no 12; falta CM-84. Corrige lo que ya sabemos por la respuesta del PO del
11-sep: los Roles Objetivo se gestionan dentro del formulario y la pantalla PRT-02.07 sale del
MVP; las expectativas salen; CM-61 cubre experiencia y educación, y la educación es obligatoria
para activar el perfil.

§12 · Reescríbela con lo resuelto y lo que sigue abierto.
Resuelto: estados de sesión oficiales CONFIGURADA, EN_CURSO, EVALUANDO, FINALIZADA, ABANDONADA;
idioma de sesión en BCP-47 con es-CO por defecto; estados de perfil IN_PROGRESS y COMPLETED, sin
PENDING ni IN_REVIEW en el flujo manual; la voz aparece visible pero deshabilitada con
"Próximamente" y el texto queda preseleccionado; el video queda fuera del MVP y se muestra
igual, deshabilitado.
Abierto, y no lo resuelvas por tu cuenta: de dónde sale el contrato de la API, si la identidad
por cabecera X-User-Id es definitiva, los valores del nivel de habilidad, los textos en español
de los niveles educativos, y si los enumerados de Entrevistas pasan a inglés.

--- D. Sección nueva: contrato con las especificaciones ---

Cada feature tiene un SPEC.md en su carpeta. Antes de tocar código de una feature se lee su
SPEC. Si el cambio altera el comportamiento descrito ahí, se actualiza el SPEC en el mismo
commit, y el humano lo aprueba en el Pull Request.

Reglas de autoridad, y esto es importante que quede escrito: el comportamiento lo fija el
backlog; el diseño lo fija Frontend. Cuando el SPEC y Figma difieran en diseño, manda el SPEC y
Figma se actualiza después. Cuando difieran en comportamiento, manda el backlog siempre.

Apunta a docs/_plantilla-feature/SPEC.md como la forma, a docs/GLOSSARY.md como la tabla de
vocabulario, a docs/adr/ como la memoria de decisiones, y a docs/decisiones/ como el registro de
lo que el PO resolvió por fuera del backlog.

--- E. Al terminar ---

Dame la lista de lo que quedó como pregunta, y la lista de reglas del archivo que describen algo
que todavía no existe en el código, con el paso que las va a resolver.

**Resultado.** §14 (Estándares de código), §15 (TSDoc) y §16 (Contrato con las especificaciones)
añadidos a `CLAUDE.md`; §5–§13 corregidas; §17 (Pendientes) creada con las reglas que describían
código todavía inexistente. Informe completo de evidencia en
`docs/bitacora-ia/CM-100-convenciones-observadas.md` (universo declarado: 121 archivos fuente +
51 de prueba).

**Qué se aceptó, corrigió o rechazó, y por qué.** El usuario corrigió la ubicación propuesta para
el informe de evidencia (`docs/referencias/` → `docs/bitacora-ia/`, porque es un artefacto de
sesión asistida por IA sin original externo, no una copia de un documento externo) y pidió
varias correcciones puntuales antes de aceptar el plan: mantener las llaves BCP-47 con guion
(no son kebab-case, son código de dominio copiado tal cual), no dividir la sección de hooks tal
como se había propuesto, conservar los puntos de §12 tal como estaban, no borrar
`ProfileRolesPage.tsx` (pendiente de C-04), diferir la corrección de `ARCHITECTURE.md` a un paso
posterior, y no dar por cumplida la regla de TSDoc "en TODOS los archivos" solo porque el informe
la citara. Ninguna parte del plan se rechazó de fondo.

**Responsable de la comprobación.** Juan Diego Gómez Garcés (autor); revisión humana pendiente
hasta el Pull Request.

---

## Tarea 2 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5) · Resolver seis pendientes de CLAUDE.md §17

**Petición/prompt relevante (resumen).** Resolver seis puntos listados en `CLAUDE.md` §17: crear
el token de diseño `--touch-target` (44px) y reemplazar los 8 literales `44px` que ya existían en
`Button`, `Input`, `Checkbox`, `Radio`, `Toggle`, `Combobox` y `PasswordField`; borrar
`src/i18n/locales/en/` para que `fallbackLng` opere de verdad en vez de simularse; reestructurar
`errors.json` con un sub-objeto `codigos`; eliminar el barril `src/stores/index.ts` y corregir sus
consumidores; corregir dos líneas desactualizadas de `docs/ARCHITECTURE.md`.

**Resultado.** Los seis puntos resueltos. Token añadido en `semantic.css`/`index.css`; 8 usos de
`44px` reemplazados por `touch-target`/`min-h-touch-target`; `src/i18n/locales/en/` borrado (5
archivos) y `i18n/index.ts`, `i18n/config.ts`, `i18n/README.md` actualizados; `errors.json`
reestructurado con `codigos.NOT_FOUND`/`VALIDATION_ERROR`/`UNAUTHORIZED` y `NotFoundPage.tsx`
ajustado a la nueva llave; `src/stores/index.ts` borrado y sus 5 consumidores (`AuthProvider.tsx`
y su prueba, `RequireAuth.tsx` y su prueba, `router/index.test.tsx`) importando directamente
`@/stores/auth.store`; `ARCHITECTURE.md` corregido (línea de `ReportLayout.tsx` y línea de
herencia de `en/`). Verificación completa (`format:check`, `typecheck`, `lint`, `test:coverage`,
`build`) pasó tras el cambio.

**Qué se aceptó, corrigió o rechazó, y por qué.** El usuario pidió previamente tres
confirmaciones antes de escribir código (área táctil de `Button` tamaño `sm` a 34px, universo de
medición declarado, alcance del token de área táctil) y las resolvió todas antes de autorizar el
paso; no hubo rechazos durante la ejecución.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.

---

## Tarea 3 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5) · Crear `docs/_plantilla-feature/`

**Petición/prompt relevante (resumen).** Crear el esqueleto copiable de feature que describe
`docs/ARCHITECTURE.md` §3: un `README.md` por subcarpeta (`api/`, `model/`, `schemas/`, `hooks/`,
`organisms/`, `pages/`, `store/`), `routes.tsx` e `index.ts` de ejemplo en la raíz, y un
`README.md` raíz explicando el propósito de la plantilla.

**Resultado.** Plantilla creada completa. El usuario detectó un error factual antes de aceptar el
paso: varias `README.md` etiquetaban como «Ejemplo real» nombres de archivo que en realidad solo
existían en el árbol ilustrativo y aspiracional de `docs/ARCHITECTURE.md` §3 (bloque
«interview-setup»), no en disco.

**Qué se aceptó, corrigió o rechazó, y por qué.** Se corrigieron las seis `README.md` señaladas
(`api`, `model`, `schemas`, `hooks`, `organisms`, `store`): «Ejemplo real» pasó a «Ejemplo
planeado…, no existe todavía en disco», con la línea exacta de `ARCHITECTURE.md` citada y
verificación por `find`/`grep` de que no hay coincidencia real. `organisms/README.md` pasó de
citar 3 de los 5 organismos planeados a los 5 completos, para no dejar una selección silenciosa
sin decirlo. `pages/README.md`, `routes.tsx`, `index.ts` y el `README.md` raíz no se tocaron en la
corrección porque sus ejemplos sí eran reales.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.

---

## Tarea 4 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5) · Capa de mocks MSW para Perfil Profesional

**Petición/prompt relevante (resumen).** Construir `src/mocks/` con MSW: identidad simulada
(`auth.handlers.ts`), ciclo completo de perfiles (`profiles.handlers.ts`) con las reglas de
negocio de nombre (CA-2.2.1 a CA-2.2.3) y educación obligatoria para finalizar (HU-2.4),
catálogos simulados (`data/catalogs.ts`), cableado de MSW (`browser.ts`/`server.ts`) y una prueba
de humo del ciclo completo, todo alcance Node/pruebas — sin service worker de navegador ni
cambios en `main.tsx`/`App.tsx`/`public/`.

**Resultado.** Capa creada. Se identificó una limitación técnica real y se resolvió con el
usuario por pregunta explícita: `auth.handlers.ts` da identidad simulada pero no puede
desbloquear el guard real `RequireAuth`, que depende del SDK de Firebase, no de MSW — se optó por
un diseño «identidad, no navegación», documentado en el TSDoc del archivo. Se detectó y corrigió
un bug real durante la prueba de humo: `POST /api/v1/profiles` validaba `name.length > 0` en vez
de `typeof name === 'string'`, dejando pasar `{ name: '' }` sin error.

**Qué se aceptó, corrigió o rechazó, y por qué.** El usuario pidió tres decisiones por pregunta
explícita (identidad sin navegación; nombre en inglés `catalogs.ts`; sin service worker de
navegador por ahora) y cuatro ajustes de redacción antes de escribir código (quitar comentario de
`GET /api/v1/users/me`; fijar 201 para la creación, no «201/200»; marcar `PROFILE_NAME_INVALID` y
`EDUCATION_REQUIRED` como códigos de mock no confirmados con backend; citar la fuente en cada
aserción de la prueba de humo) — todos aplicados. En la auditoría de cinco puntos que pidió el
usuario al cerrar el paso, se detectaron y corrigieron dos citas desactualizadas («cinco capas» →
«seis capas» en `docs/ARCHITECTURE.md` §4 y en el comentario de `eslint.config.js`) que quedaron
así tras añadir la política `boundaries/dependencies` para `mocks → services`.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.

---

## Tarea 5 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5) · `pnpm spec:check` y `docs/SPEC-INDEX.md`

**Petición/prompt relevante (resumen).** Construir `scripts/spec-check.mjs` (parser de
front-matter propio, sin dependencias nuevas; reglas SC-01 a SC-05) y `docs/SPEC-INDEX.md`
regenerado, replicando el patrón `continue-on-error: true` + `::warning::` de los workflows de
Paula (`validar-nombre-rama.yml`, `validar-plantilla-pr.yml`), en un workflow nuevo
(`.github/workflows/validar-specs.yml`) separado de `ci.yml`, con la misma fecha límite
18-sep-2026.

**Resultado.** `scripts/spec-check.mjs`, `docs/SPEC-INDEX.md`, `.github/workflows/validar-specs.yml`
y la entrada `spec:check` en `package.json`, todos creados. La verificación completa pasó tras dos
ajustes de lint en `eslint.config.js`: el glob `**/*.mjs` añadido al bloque
`disableTypeChecked` (el script no pertenece a ningún `tsconfig`) y `languageOptions.globals:
globals.node` añadido al mismo bloque (`process`/`console` disparaban `no-undef`).

**Qué se aceptó, corrigió o rechazó, y por qué.** El usuario pidió dos ajustes antes de escribir
código: id corto por regla (`SC-01`..`SC-05`) con un formato de mensaje de fallo específico, y que
los tokens de la tabla §7 sin `/` (no verificables por SC-04) fueran a una lista aparte en vez de
descartarse en silencio — ambos aplicados tal cual. Los dos cambios de `eslint.config.js` se
divulgaron explícitamente como una desviación del alcance de archivos que el usuario había
autorizado para este paso (`scripts/`, `package.json`, `docs/SPEC-INDEX.md`,
`.github/workflows/validar-specs.yml`).

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.

---

## Tarea 6 · 11-sep-2026 · Herramienta: Claude Code (Claude Opus 5 / Sonnet 5) · `SPEC.md` piloto de `professional-profile`

**Petición/prompt relevante (resumen).** Escribir `src/features/professional-profile/SPEC.md`
como la primera instancia de la plantilla, citando datos de backlog no versionados que el usuario
proporcionó directamente en el prompt (con su fuente), sin inventar ningún dato faltante —
cualquier vacío va a la sección 8 de bloqueos.

**Resultado.** `SPEC.md` creado; `pnpm spec:check` dio 0 errores, incluida la predicción explícita
de que SC-03 pasaría (las tres rutas de `rutas:` aparecen literalmente citadas en
`app/router/routes.ts`), confirmada contra la salida real. Durante la verificación posterior
(`pnpm format:check` sobre todo el repositorio, no solo los dos archivos del paso) apareció un
documento real que la búsqueda inicial no había encontrado:
`docs/referencias/11092026_v1_consulta-contrato-y-alcance-perfil-profesional.md` — la «consulta de
seguimiento» que el usuario había citado y que el buscador por nombre (`*seguimiento*`) no
detectó porque esa palabra solo está en el título del documento, no en su nombre de archivo. Esto
obligó a corregir tres cosas ya escritas: la cita en `decisiones:` del front-matter, las siete
filas de §8 (que decían «no registrado en el repositorio» para C-02/C-03/C-05, cuando sí tenían
enunciado real), y a añadir una observación nueva en §9 sobre la ubicación del documento.

**Qué se aceptó, corrigió o rechazó, y por qué.** El usuario resolvió por pregunta explícita tres
bloqueos antes de escribir: el documento de seguimiento, en ese momento no localizado, se incluye
igual en el front-matter con una nota de bloqueo (en vez de omitirlo); C-02/C-03/C-05 sin
enunciado localizado se listan con «enunciado no disponible» en vez de inventarlo; los Roles
Objetivo del §3 registran D-01 (gestión completa en el formulario) como destino, con el diseño de
solo-lectura + enlace a `PRT-02.07` marcado explícitamente como provisional, no como el
comportamiento definitivo que el prompt original describía (esa lectura era la opción 1 de D-01,
la que el PO rechazó). Tras aparecer el documento real, el usuario pidió verificar carácter por
carácter si dos de las tres entradas de `decisiones:` colisionaban (`10092026_v1` /
`11092026_v2` / `11092026_v1`): se confirmó, con comparación programática, que las tres cadenas
son distintas, y no se aplicó ninguna corrección porque la condición del usuario no se cumplió.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.

---

## Tarea 7 · 11-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Verificación final antes del Pull Request

**Petición/prompt relevante (resumen).** Verificación completa de CM-100 antes de abrir el Pull
Request hacia `develop`, sin escribir código nuevo: batería de comandos (`format:check`,
`typecheck`, `lint`, `test:coverage`, `build`, `spec:check`); consistencia documental de
`CLAUDE.md` §17, `docs/ARCHITECTURE.md`, `docs/SPEC-INDEX.md` y esta bitácora; higiene completa
de `git status --porcelain`; y redacción (sin publicar) del contenido del Pull Request.

**Resultado.** Ver el reporte completo de esta misma tarea en la conversación: verificación sin
regresiones (cobertura idéntica a la del cierre de la Tarea 5), dos inconsistencias documentales
encontradas en `CLAUDE.md` §16/§17 (no corregidas en este paso porque la instrucción fue reportar,
no corregir) y esta bitácora completada con las entradas de las Tareas 2 a 6, que faltaban.

**Qué se aceptó, corrigió o rechazó, y por qué.** Pendiente de que el usuario revise el reporte
de este paso.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request.
