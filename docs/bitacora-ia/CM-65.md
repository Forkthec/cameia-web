## Tarea 1 · 15-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Habilidades y botón «Finalizar y Continuar» (HU-2.5)

**Petición/prompt relevante (resumen).** Construir la sección Habilidades (texto libre + nivel,
gestión por ítem) y conectar el botón «Finalizar y Continuar» a un endpoint real, dentro del mismo
patrón arquitectónico que `EducationSection`/`WorkExperienceSection` (CM-61). El usuario adjuntó
directamente el código real del backend (`ProfileController.java`, `ProfessionalRoleController.java`,
`OpenApiConfig.java`, de `cameia-perfil`) como fuente de mayor autoridad. Se autorizó explícitamente
no revisar el Figma nodo por nodo para esta tarea. Rama independiente de `CM-69-roles-objetivo-catalogo`
(construida antes en esta misma sesión): ambas parten de `develop`, no una de la otra — decisión
tomada por el usuario tras confirmar que CM-61 ya estaba fusionado.

**Resultado.** `SkillsSection` construida completa (texto libre + nivel, mostrada como `Chip`,
duplicado bloqueado en cliente con `setError` manual), 3 hooks (`useAddSkill`, `useRemoveSkill`,
`useFinalizeProfile`), extensión de `model/` (`SkillItem`, `SkillLevel`, y `TargetRoleItem` de solo
lectura), `api/` (DTO, mapper, funciones de red) y mocks (2 handlers por ítem de habilidades +
reemplazo del endpoint de finalización). `ProfileActionsBar` conecta de verdad «Finalizar y
Continuar» (`onFinish`/`isFinalizing`/`finalizingLabel`, antes solo un botón muerto). `model/missingRequirements.ts`
extrae la lectura de `error.details` a una función pura, testable sin renderizar. `SPEC.md`
reescrito (§2, §3, §3.4, §4, §5, §6, §7, §8, §9) y `GLOSSARY.md` §2/§6 actualizados. Verificación
completa: `pnpm typecheck` (0 errores), `pnpm lint` (0 errores, mismos 2 warnings preexistentes
documentados como no bloqueantes), `pnpm test` (291/291 pruebas), `pnpm spec:check` (0 errores).

**Qué se aceptó, corrigió o rechazó, y por qué.**

- El código real del backend, compartido en la sesión, resultó más autoritativo que el memo del
  13-sep y muy por encima de `SPEC.md`/`GLOSSARY.md` (que seguían sin actualizar): reveló que el
  endpoint de finalización real es `POST .../completion`, no `POST .../finalize` como el mock
  simulaba desde CM-61 — corregido directamente en esta rama (a diferencia de CM-69, que solo lo dejó
  documentado como bloqueo `C-16` porque no era su alcance tocarlo). También confirmó
  `SkillLevel = BASIC/INTERMEDIATE/ADVANCED` y los nombres de campo de `AddSkillRequest`, cerrando el
  bloqueo **C-06** de `SPEC.md`.
- **Decisión de diseño explícita, sin necesidad de preguntar (documentada como decisión I3,
  `SPEC.md` §9):** dado que HU-2.5 ya pedía "verificar que exista al menos un rol objetivo antes de
  finalizar", esta rama necesita `Profile.targetRoles` de solo lectura aunque no construya su
  gestión (eso es CM-69, en paralelo). Se modeló con la forma exacta que usa CM-69
  (`TargetRoleItem { id, professionalRoleId, provenance }`, no un `string[]` simplificado) para que
  ambas ramas coincidan en ese campo y no generen conflicto de tipos al fusionarse — la
  reconciliación real pendiente es unir `getCompletenessValue`/`getSectionStatuses` (hoy cada rama
  calcula 4 de 5 por separado) en una función que sume los 5 requisitos juntos.
- **Limitación de pruebas documentada, no evitada:** `features` no puede importar `mocks`
  (`docs/ARCHITECTURE.md` §4), y sin los endpoints de Roles Objetivo de CM-69 esta rama no tiene
  forma pública de construir un perfil que cumpla los 5 requisitos reales. El caso de éxito de
  `useFinalizeProfile` se prueba con un handler de MSW puntual (`useHandlers`); el caso de éxito del
  endpoint completo (con los 5 requisitos reales) se prueba aparte, con `seedProfileForTests`, en
  `mocks/handlers/profiles.handlers.test.ts` (infraestructura, no feature — sí puede importar el
  propio módulo que prueba). El botón real «Finalizar y Continuar» queda siempre deshabilitado en
  esta rama en solitario porque nunca ve el 5º requisito; se prueba explícitamente ese estado en vez
  de forzar una interacción imposible.
- **Verificación de UI en navegador incompleta**, mismo motivo que en CM-69: no hay `chromium-cli`
  disponible y Playwright está explícitamente prohibido (`CLAUDE.md` §2). Se confirmó por `curl` que
  el servidor de desarrollo sirve la app y que Vite transforma `SkillsSection.tsx`/`EditProfilePage.tsx`
  sin errores de compilación, pero no se pudo conducir la interacción real (agregar/quitar una
  habilidad, intentar finalizar) en un navegador real. Queda pendiente de verificación manual por
  quien revise el Pull Request.
- **Conflicto de fusión esperado, documentado con todas las letras (no oculto):** ambas ramas tocan
  `EditProfilePage.tsx`, `profileCompleteness.ts`, `profile.types.ts`, `profile.constants.ts`,
  `profiles.handlers.ts`, `errors.json` y `profile.json` de forma real y distinta (CM-69 agrega la
  sección de Roles Objetivo con su UI completa; CM-65 agrega Habilidades y conecta Finalizar). Quien
  fusione la segunda de las dos ramas debe reconciliar manualmente esos archivos — no es un error de
  esta sesión, es la consecuencia esperada de la decisión explícita del usuario de mantenerlas como
  ramas independientes en paralelo, ya adelantada al usuario al confirmar esa decisión (§0 de su
  propio prompt: "por defecto, sepáralas").

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request, incluida la verificación manual en navegador y la reconciliación de la fusión con
CM-69 que esta sesión no pudo completar.
