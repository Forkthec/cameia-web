## Tarea 1 · 15-sep-2026 · Herramienta: Claude Code (Claude Sonnet 5) · Sección «Roles Objetivo» (HU-2.11)

**Petición/prompt relevante (resumen).** Construir la gestión completa de Roles Objetivo dentro del
Formulario de Perfil Profesional (consultar, agregar, sustituir, eliminar; catálogo cerrado, máximo
5, sin prioridad ni reordenamiento), siguiendo el mismo patrón arquitectónico que
`EducationSection`/`WorkExperienceSection` (CM-61): sin `useFieldArray`, alta/baja/sustitución como
mutaciones inmediatas contra la caché de `useProfileQuery`. El usuario adjuntó directamente el
código real del backend (`ProfileController.java`, `ProfessionalRoleController.java`,
`OpenApiConfig.java`, de `cameia-perfil`) como fuente de mayor autoridad, por encima del memo del PO
del 13-sep y muy por encima de `SPEC.md`/`GLOSSARY.md`, que en ese momento seguían sin actualizar
tras la respuesta del PO. Se autorizó explícitamente no revisar el Figma nodo por nodo para esta
tarea (guía visual desactualizada, no fuente de comportamiento ni de diseño vinculante).

**Resultado.** Sección construida completa: `TargetRolesSection` (reutiliza `Combobox` para agregar
y `Select` por ítem para sustituir — ningún átomo/molécula nuevo de `design-system`), 4 hooks
(`useAddTargetRole`, `useSubstituteTargetRole`, `useRemoveTargetRole`, `useProfessionalRolesQuery`),
extensión de `model/`, `api/` (DTO, mapper, funciones de red) y mocks (3 handlers por ítem de Roles
Objetivo reemplazando el `PATCH` masivo de `targetRoleIds` que solo existía por no conocer el
contrato, más un handler nuevo del catálogo `GET /api/v1/professional-roles`), i18n completo, y
`profileCompleteness.ts` extendido para contar el cuarto de los 5 requisitos de finalización.
`SPEC.md` reescrito (§2, §3, §3.5, §4, §5, §6, §7, §8, §9) y `GLOSSARY.md` §2 actualizado para
reflejar el contrato real confirmado por el código del backend. Verificación completa:
`pnpm typecheck` (0 errores), `pnpm lint` (0 errores, mismos 2 warnings preexistentes documentados
como no bloqueantes), `pnpm test` (294/294 pruebas, incluidas 9 nuevas de `TargetRolesSection`, 6 de
hooks nuevos, 12 de handlers de mocks nuevos, 2 de `profile.mapper.ts`, 2 de `profileCompleteness.ts`
y 2 de integración en `EditProfilePage.test.tsx`), `pnpm spec:check` (0 errores).

**Qué se aceptó, corrigió o rechazó, y por qué.**

- Se preguntó explícitamente el orden de ramas dado que CM-61 no aparecía fusionado en el
  `develop` local; el usuario aclaró que sí estaba fusionado (PR #34, commit `c8cf47e`) y que solo
  faltaba sincronizar el `develop` local — confirmado con `git fetch`/`merge-base` antes de crear la
  rama. Sin ese hallazgo, se habría apilado innecesariamente sobre CM-61.
- El código real del backend, compartido en la sesión (no accesible a los subagentes de exploración,
  que no tienen el historial de la conversación), resultó más autoritativo que los tres documentos
  del repositorio: reveló que el endpoint de finalización real es `POST .../completion`, no
  `.../finalize` como simulaba el mock desde CM-61 (documentado como bloqueo **C-16**, a resolver en
  CM-65, no en esta rama); que Roles Objetivo tiene endpoints reales por ítem (`POST`/`PATCH`/
  `DELETE`), no el `PATCH` masivo que el mock simulaba; y que el catálogo de roles sí tiene endpoint
  real (`GET /api/v1/professional-roles`), contra lo que decía `SPEC.md` §5 hasta este commit.
- Se decidió, sin necesidad de preguntar, construir CM-69 antes que CM-65 (orden inverso al de la
  numeración de tickets): el botón «Finalizar y Continuar» de CM-65 necesita que las 5 secciones
  existan para poder probarse de punta a punta: con CM-69 primero, CM-65 hereda una tercera sección
  ya construida en vez de simularla a medias.
- No se tocó `ProfileRolesPage.tsx` ni su ruta `/perfiles/:id/roles`: instrucción explícita del
  usuario de conservarlos hasta que el PO confirme el destino de HU-2.10 (Sprint 2), tal como ya
  documentaba `CLAUDE.md` §12.
- **Verificación de UI en navegador incompleta.** Se inició `pnpm dev` y se confirmó por `curl` que
  el servidor sirve la aplicación y que Vite transforma `TargetRolesSection.tsx` sin errores de
  compilación, pero no se pudo conducir la interacción real (agregar/sustituir/eliminar un rol en un
  navegador) porque no hay una herramienta de automatización de navegador disponible en este entorno
  (no existe `chromium-cli`; Playwright está explícitamente prohibido por `CLAUDE.md` §2, «no
  instales nada nuevo… ni Playwright»). La cobertura de comportamiento real del flujo (clic en el
  combobox, filtrado, selección, sustitución, eliminación, bloqueo del último rol) queda en manos de
  `TargetRolesSection.test.tsx` y `EditProfilePage.test.tsx` (Testing Library sobre DOM real vía
  jsdom, no una aserción de snapshot), no de una prueba de navegador real. Queda pendiente de
  verificación manual por quien revise el Pull Request.

**Responsable de la comprobación.** Juan Diego Gómez Garcés; revisión humana pendiente hasta el
Pull Request, incluida la verificación manual en navegador que esta sesión no pudo completar.
